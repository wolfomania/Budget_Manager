spring:
  datasource:
    url: jdbc:mysql://localhost:3306/TPO_test
    username: user
    password: password

  jpa:
#    hibernate:
#      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true